DB_PROVIDER=mysql
DB_HOST=localhost
DB_NAME=biometric_attendance
DB_USER=root
DB_PASSWORD=Surana#123

# Use long random secrets (32+ bytes recommended)
SECRET_KEY=change-me-change-me-change-me-change-me
JWT_SECRET_KEY=change-me-change-me-change-me-change-me

# Optional: use a full SQLAlchemy URL instead of the DB_* vars
# DATABASE_URL=mysql+pymysql://user:pass@localhost/dbname

# Email (Flask-Mail / SMTP)
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=your-email@example.com
MAIL_PASSWORD=your-app-password

# Email notifications
# SEND_LEAVE_STATUS_EMAIL=true
# LEAVE_STATUS_EMAIL_ASYNC=true
# SEND_JUSTIFICATION_STATUS_EMAIL=true
# JUSTIFICATION_STATUS_EMAIL_ASYNC=true

# CORS (development)
# Allow all origins temporarily (NOT for production):
# CORS_ALLOW_ALL=true
# Or restrict to comma-separated origins:
# CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
